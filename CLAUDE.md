# CLAUDE.md
# jimitas-new プロジェクト指示書

## このプロジェクトについて

**jimitas.com のリニューアルプロジェクト**

「地味に助かる」をコンセプトにした、先生・子ども・保護者向けの学習アプリポータル。
バラバラに存在していたアプリを1つのNext.jsプロジェクトに統合する。

詳細は `docs/` フォルダのドキュメントを参照してください。
- `docs/01_要件定義書.md` ← 何を作るか
- `docs/02_設計書.md` ← どう作るか
- `docs/03_アプリ一覧.md` ← アプリの棚卸し結果
- `docs/04_コンポーネント設計.md` ← 部品の設計

---

## オーナーについて

- 現役の先生・個人開発者
- HTML/CSS/JS 経験あり
- Next.js + TypeScript は学習中（バイブコーディングで進める）
- **コードの説明を適宜入れること**（何をしているか理解しながら進めたい）
- 一度に大量のコードを出さず、ステップごとに確認しながら進めること

---

## 開発方針

- Next.js（App Router）+ TypeScript + Tailwind CSS
- 部品化・再利用を重視する
- 新しいアプリは `src/data/apps.ts` に1行追加するだけで追加できる設計にする
- ゲーミフィケーション（コイン）は当面 localStorage、将来 Supabase に移行
- `jimitas-old/` に参照用の既存アプリがある（移植時に参照する）

---

## ディレクトリ設計とすみわけ基準

```
src/
├── app/apps/[app-name]/page.tsx   ← アプリのページ
└── components/
    ├── common/                    ← サイト全体共通（Header, Footer など）
    └── parts/                     ← 再利用パーツ
        ├── buttons/               ← ボタン系
        ├── displays/              ← 表示系
        └── block/                 ← ブロック系
```

### `src/app/apps/[app-name]/page.tsx` に書くもの（インライン）
- そのアプリ固有の状態管理（useState, useRef）
- そのアプリ固有のイベントハンドラー・計算ロジック
- ページのレイアウト・JSX
- **他のアプリで再利用しないもの**はすべてここに置く

### `src/components/parts/` に置くもの
- **2つ以上のアプリで使う**再利用パーツ
- アプリのビジネスロジックを持たない単独のUI部品
- 今後も使い回す見込みのあるもの

### 禁止事項
- `src/components/apps/` は作らない（廃止済み）
- あるアプリ専用のコンポーネントを `components/` に置かない
- ページ固有のロジックを無理に部品化しない（過剰な抽象化を避ける）

---

## 注意事項

- TypeScript の型エラーは必ず解消すること
- Tailwind CSS でスタイリングする（独自CSSは極力使わない）
- レスポンシブ対応必須（タブレット・PC）
- コメントは日本語で書くこと

---

## スクリーンショット駆動でビジュアル確認

大きな視覚変更（配色リファクタリング・レイアウト変更・UI 統一など）を行ったあとは、
**Playwright で全アプリのスクショを撮って Claude Code が画像を目視チェックする**ワークフローを使う。

### なぜこれが効くか

- grep だけでは「典型パターン」しか見つけられない（例: `bg-red-500 hover:bg-red-600`）
- 画像を実際に目で見ることで「トーンが浮いている」「色が違う」を直感的に検出できる
- Claude Code はマルチモーダル LLM なので、生成された PNG を `Read` ツールで視覚的に解釈できる
- 2026-04-26 の配色リファクタリングで実際に取りこぼし 6 箇所を発見した

### 使い方

```bash
# 1. 別ターミナルで dev server を立ち上げる（まだなら）
npm run dev

# 2. 全アプリのスクショを一括取得
npm run screenshot
# → screenshots/<日付>/ に PNG が生成される（全 53 アプリ + トップページ）
```

### 撮影スクリプト

`scripts/screenshot-all.ts` に実装。`apps.ts` から自動でルート一覧を取得する。
- viewport: 1024×768（タブレットファースト）
- 環境変数 `BASE_URL` で対象 URL を変更可能（本番チェックにも使える）
- 環境変数 `VIEWPORT=mobile` でスマホビュー（375×812）に切り替え

### Claude Code 側のワークフロー

1. リファクタリング後に `npm run screenshot` を実行してもらう
2. `screenshots/<日付>/` の主要画像を `Read` で確認（全 54 枚見ると context が膨れるので、変更が大きいアプリを優先）
3. トーンが浮いている箇所を発見したら、該当ファイルを `Grep` + `Edit` で修正
4. もう一度 `npm run screenshot` で再撮影 → before/after 比較

### 見るべき優先順位の例

- トップページ（`00_index.png`）: 全体のトーンの基準
- 変更が大きかったアプリ（共通部品・モード切替など）
- インライン書きが多いアプリ（筆算・タイマー系）
- 元々 raw color が混在していたアプリ

### 出力先（gitignore 済み）

`screenshots/` は `.gitignore` に登録済み。容量が大きいのでリポジトリには含めない。
バージョン別ベースラインを残したいときは別フォルダにコピーするか、画像 1 枚だけ docs/ に貼る。

### 詳しい経緯

`docs/report_2026-04-26_スクショ駆動検証.md` 参照。

---

## スキル自動起動ルール

以下のパターンの指示を受けたとき、**必ず**対応するスキルを呼び出してから作業を始めること。

| 指示パターン（例） | 起動するスキル |
|------------------|--------------|
| 「○○のアプリを作りたい」「新しいアプリを追加」「○○を作って」 | `/new-app` |
| 「jimitas-old から移植」「旧アプリを移植」「○○を移植して」 | `/port` |
| 「一段落した」「コミットして」「push して」「リリース」 | `/ship` |
| 「見た目を確認」「スクショで確認」「ビジュアルチェック」「配色確認」 | `/screenshot-review` |
| 「効果音を確認」「SE チェック」「カーソルを確認」 | `/se-audit` |
| 「新アプリの SEO」「seoDescription を確認」 | `/seoaio` |
