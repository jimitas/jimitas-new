# ハンドオーバー（2026-03-22）

新しい会話を始めるときに最初に読んでください。

---

## 直前の作業内容

### 筆算アプリ 3本のUX改善（完了）

`tashi-hissan`・`hiki-hissan`・`kake-hissan-1` の3アプリに以下を実装した。

| 機能 | tashi | hiki | kake-1 |
|---|:---:|:---:|:---:|
| 筆算テーブル D&D | ✅ | ✅ | ✅ |
| お金テーブル D&D | ✅ | ✅ | ✅（OkaneGrid） |
| ゴミ箱キャンセル音 | ✅ | ✅ | ✅ |
| お金操作ヒント（薄い透過テキスト） | ✅ | ✅ | ✅ |
| せいかい！🎉（正解演出） | ✅ | ✅ | ✅ |
| コイン重複防止（useProblemCoins） | ✅ | ✅ | ✅ |
| くり上がり行の数字リサイズ | — | — | ✅ |

### 重要な実装上の注意（次の作業でも守ること）

**筆算テーブル（tblRef）のセルに span を入れてはいけない**
`kotaeInput()` が `cell.innerText` で数値を読み取るため、テキストを持つ要素が混入すると答えが NaN になる。
ヒントはお金テーブル（tbl2Ref）側のセルにのみ追加すること。

---

## 現在のファイル構成（主要部分）

```
src/app/(apps)/
├── tashi-hissan/page.tsx   ← たし算ひっ算（完成）
├── hiki-hissan/page.tsx    ← ひき算ひっ算（完成）
└── kake-hissan-1/page.tsx  ← かけ算ひっ算①（完成）

src/components/parts/hissan/
└── OkaneGrid.tsx           ← kake-hissan-1 専用のお金グリッド（variant="kake1"）

src/hooks/
└── useProblemCoins.ts      ← 正解重複防止フック（筆算3本で使用）
```

---

## 次にやること（候補）

以下の中からユーザーに確認して進める。

### 優先度 高
- **kake-hissan-2（かけ算の筆算②）の移植**
  旧ファイル: `jimitas-old/motto/16kah2.js`
  難易度：高（小数対応あり）
  `kake-hissan-1` の実装を踏まえて設計する。

- **git push & Vercel デプロイ確認**
  ここ数回の作業分がまだ push されていない可能性がある（要確認）。

### 優先度 中
- **warizan（わり算の考え方①②）の移植**
  旧ファイル: `jimitas-old/motto/17war1.js`, `17war2.js`
  難易度：中

- **wari-hissan（わり算の筆算）の移植**
  旧ファイル: `jimitas-old/motto/18warih.js`
  難易度：高（6段階難易度・ドラッグ&ドロップ）

- **apps.ts への筆算アプリ3本の追加**
  `src/data/apps.ts` に `tashi-hissan`・`hiki-hissan`・`kake-hissan-1` のエントリが入っているか確認。

### 優先度 低
- じみぷり（算数プリント自動生成）の移植
- カタカナ・ローマ字練習の移植

---

## メモリへのポインタ

詳細な設計・パターンは以下のメモリファイルに保存済み。
新しい会話でも自動的に読み込まれる。

| ファイル | 内容 |
|---|---|
| `memory/project_hissan.md` | 筆算3本の全アーキテクチャ詳細 |
| `memory/feedback_seikai_display.md` | 正解演出パターン（seikaiRef）|
| `memory/feedback_coin_dedup.md` | コイン重複防止パターン |
| `memory/project_parts_structure.md` | parts/・hooks/の共通部品一覧 |

---

## git 状態（2026-03-22 時点の未コミット分）

```
M  src/app/(apps)/hiki-hissan/page.tsx    ← ヒント・せいかい！追加
M  src/app/(apps)/tashi-hissan/page.tsx   ← ヒント・せいかい！追加
M  src/data/apps.ts                       ← 要確認
?? src/app/(apps)/kake-hissan-1/          ← 新規（全ファイル未コミット）
?? src/components/parts/hissan/           ← OkaneGrid 新規
?? public/sounds/cancel2.mp3              ← 音声ファイル
?? public/sounds/cancel3.mp3             ← 音声ファイル
```

新しい会話を始める前に `git add` & `git commit` しておくと履歴が綺麗になる。
